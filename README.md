# Design Adventure Portfolio

黑底编辑式个人作品集骨架。当前项目区域使用可替换的灰色卡片占位，首屏、个人卡与收尾使用提供的素材。

直接双击 `index.html` 即可预览；无需安装依赖。

## 内容替换

- 项目卡片：在 `index.html` 中搜索 `placeholder`。
- 联系邮箱：搜索 `hello@example.com` 并替换。
- 图片与字体：位于 `assets/`。
